pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        maven { url 'https://repo.spring.io/plugins-release' }
    }
}

rootProject.name = 'santander-dev-week-2024'
